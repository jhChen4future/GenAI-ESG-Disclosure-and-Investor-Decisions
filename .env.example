# Required only for the AI-assisted investor interface.
BIANXIE_API_KEY=replace_with_your_own_key

# Required for a non-local oTree deployment.
OTREE_ADMIN_PASSWORD=replace_with_a_strong_password
OTREE_SECRET_KEY=replace_with_a_random_secret
